# Vite dev server proxies /api to the backend (see vite.config.ts), so this is
# only needed if you point the frontend at a different backend host.
VITE_API_URL="http://localhost:4000"
VITE_SOCKET_URL="http://localhost:4000"
